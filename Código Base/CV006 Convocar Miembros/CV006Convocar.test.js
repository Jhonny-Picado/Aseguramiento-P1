test("dummy", () => {
  const res = () => {
    const json = (x) => {};
    const status = (x) => {
      return { json: json };
    };
    return { json, status };
  };

  expect(1).toBe(1);
});

test("dummy", () => {
  expect(1).toBe(1);
});

test("dummy", () => {
  expect(1).toBe(1);
});

test("dummy", () => {
  expect(1).toBe(1);
});
